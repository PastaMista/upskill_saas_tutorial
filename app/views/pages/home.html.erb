<!--datei muss so heißen, wie im controller deffiniert (home) -->
<div class="home-callout text-center container-fluid">
  <h1>Serwus i bims dein Kallemder</h1>
  <h3>Hier gibts Kallender vong Datum her.</h3>
  
  <%= link_to "Create your Profile!", new_user_profile_path(user_id: current_user.id) if user_signed_in? %>  <!-- Link zur Pforil Erstellung, nur sichtbar wenn eingeloggt. User ID wird durch Devise Funktion ausgelesen -->
</div>